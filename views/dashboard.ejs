<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <title>Dashboard Admin</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f1f3f5;
            margin: 0;
            padding: 0;
        }

        .navbar {
            background-color: #343a40;
            padding: 15px 20px;
            color: #fff;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .navbar h2 {
            margin: 0;
        }

        .logout-btn {
            background-color: #dc3545;
            color: white;
            padding: 8px 16px;
            border: none;
            border-radius: 4px;
            text-decoration: none;
            font-weight: bold;
            cursor: pointer;
        }

        .logout-btn:hover {
            background-color: #c82333;
        }

        .container {
            padding: 30px 50px;
        }

        .admin-info {
            display: flex;
            align-items: center;
            background-color: #ffffff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
            margin-bottom: 30px;
        }

        .admin-info img {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            object-fit: cover;
            margin-right: 20px;
            border: 2px solid #007bff;
        }

        .admin-info h3 {
            margin: 0;
            font-size: 22px;
            color: #333;
        }

        .admin-info p {
            margin: 4px 0 0;
            color: #666;
        }

        .dashboard-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
            gap: 20px;
        }

        .card {
            background-color: #ffffff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 2px 6px rgba(0,0,0,0.1);
            text-align: center;
            transition: transform 0.2s ease;
            cursor: pointer;
            position: relative; /* Thêm position relative để đặt badge */
        }

        .card:hover {
            transform: translateY(-4px);
        }

        .card h4 {
            margin: 10px 0;
            color: #007bff;
            display: flex; /* Dùng flexbox để căn chỉnh icon và badge */
            justify-content: center;
            align-items: center;
            gap: 8px; /* Khoảng cách giữa chữ và badge */
        }

        .card p {
            font-size: 14px;
            color: #555;
        }
        /* Style cho các liên kết trong card */
        .card a {
            text-decoration: none;
            color: inherit;
            display: block;
        }

        /* NEW: Style cho badge thông báo */
        .notification-badge {
            background-color: #ff4d4f; /* Màu đỏ */
            color: white;
            font-size: 12px;
            font-weight: bold;
            border-radius: 50%;
            padding: 4px 8px;
            min-width: 20px; /* Đảm bảo đủ rộng cho 1 hoặc 2 chữ số */
            height: 20px;
            display: flex;
            justify-content: center;
            align-items: center;
            position: absolute; /* Đặt badge dựa trên card */
            top: 15px; /* Điều chỉnh vị trí */
            right: 15px; /* Điều chỉnh vị trí */
            transform: translate(50%, -50%); /* Dịch chuyển để nằm ở góc */
            z-index: 10; /* Đảm bảo nằm trên các phần tử khác */
            opacity: 0; /* Mặc định ẩn */
            transition: opacity 0.3s ease-in-out;
        }

        .notification-badge.show {
            opacity: 1; /* Hiển thị khi có số > 0 */
        }
    </style>
</head>
<body>

    <div class="navbar">
        <h2>Trang Quản Trị FMCarer</h2>
        <form action="/admin/logout" method="POST" style="margin: 0;">
            <button type="submit" class="logout-btn">Đăng xuất</button>
        </form>
    </div>

    <div class="container">

        <% if (admin) { %>
            <div class="admin-info">
                <img src="<%= admin.image || 'https://media.vov.vn/sites/default/files/styles/large/public/2024-12/tin_tac_thuc_hien_vu_tan_cong_vao_cong_ty_luat_hwl_ebsword_va_thang_4_2023_va_danh_cap_nhieu_thong_tin_nhay_cam_20241205101532.png.jpg' %>" alt="Avatar" />
                <div>
                    <h3><%= admin.fullname || admin.username %></h3>
                    <p>Vai trò: <%= admin.role || 'Admin' %></p>
                </div>
            </div>
        <% } else { %>
            <h1>Không tìm thấy thông tin admin</h1>
        <% } %>

        <div class="dashboard-grid">
            <div class="card">
                <a href="/admin/users">
                    <h4>👥 Quản lý người dùng <span id="user-notification" class="notification-badge"></span></h4>
                    <p>Xem và quản lý danh sách phụ huynh</p>
                </a>
            </div>

            <div class="card">
                <a href="/admin/posts">
                    <h4>📝 Quản lý bài viết <span id="post-notification" class="notification-badge"></span></h4>
                    <p>Duyệt, chỉnh sửa hoặc xoá bài viết</p>
                </a>
            </div>

            <div class="card">
                <h4>📅 Quản lý nhắc nhở <span id="reminder-notification" class="notification-badge"></span></h4>
                <p>Thống kê các nhắc nhở quan trọng</p>
            </div>

            <div class="card">
                <a href="/admin/statistics">
                    <h4>📊 Thống kê hệ thống</h4>
                    <p>Xem tổng số người dùng, bài viết, trẻ em</p>
                </a>
            </div>

            <div class="card">
                <h4>⚙️ Cài đặt hệ thống</h4>
                <p>Cập nhật cấu hình chung, vai trò</p>
            </div>
        </div>

    </div>

    <script>
        // Hàm để lấy dữ liệu thông báo từ backend và cập nhật UI
        async function fetchNotifications() {
            try {
                // Ví dụ các API endpoint bạn cần tạo ở backend
                const userResponse = await fetch('/admin/api/notifications/users'); // API trả về số người dùng mới đăng ký/chưa được duyệt
                const postResponse = await fetch('/admin/api/notifications/posts'); // API trả về số bài viết chờ duyệt
                const reminderResponse = await fetch('/admin/api/notifications/reminders'); // API trả về số nhắc nhở cần chú ý

                const userData = await userResponse.json();
                const postData = await postResponse.json();
                const reminderData = await reminderResponse.json();

                updateNotificationBadge('user-notification', userData.count);
                updateNotificationBadge('post-notification', postData.count);
                updateNotificationBadge('reminder-notification', reminderData.count);

            } catch (error) {
                console.error('Lỗi khi tải thông báo:', error);
                // Có thể ẩn các badge nếu có lỗi để tránh hiển thị sai
                updateNotificationBadge('user-notification', 0);
                updateNotificationBadge('post-notification', 0);
                updateNotificationBadge('reminder-notification', 0);
            }
        }

        // Hàm cập nhật nội dung và hiển thị/ẩn badge
        function updateNotificationBadge(elementId, count) {
            const badge = document.getElementById(elementId);
            if (badge) {
                if (count > 0) {
                    badge.textContent = count;
                    badge.classList.add('show'); // Thêm class 'show' để hiển thị
                } else {
                    badge.textContent = '';
                    badge.classList.remove('show'); // Xóa class 'show' để ẩn
                }
            }
        }

        // Tải thông báo khi trang được load
        document.addEventListener('DOMContentLoaded', fetchNotifications);

        // Tự động tải lại thông báo mỗi 60 giây (tùy chọn)
        // setInterval(fetchNotifications, 60000);
    </script>

</body>
</html>